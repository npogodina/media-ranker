<h2>List of Works</h2>

<h4>Albums</h4>
<%= render partial: "works_table", locals: { work_type: @albums } %>

<h4>Books</h4>
<%= render partial: "works_table", locals: { work_type: @books } %>

<h4>Movies</h4>
<%= render partial: "works_table", locals: { work_type: @movies } %>

<%= link_to "View top media", welcome_path, class: "btn btn-primary" %>    
<%= link_to "Add a new work", new_work_path, class: "btn btn-primary" %>    
